* single-threaded

** TODOs

*** TODO File > Run doesn't work

Creates new Beeb, but both drives are empty...

*** TODO Save state
*** TODO Timeline UI
*** TODO Timeline general
*** TODO BeebLink
*** TODO Tracing when recording

Should cancel the trace if the beeb is rewound or a save state is
loaded. Does it already do this?

*** TODO Copy BASIC
*** DONE Power-on tone
    CLOSED: [2021-05-31 Mon 16:43]

It's always on.

*** DONE Volume control
    CLOSED: [2021-05-31 Mon 16:50]

Always max volume.

*** DONE HTTP server
    CLOSED: [2021-06-06 Sun 22:25]

*** DONE Remove HTTP example
    CLOSED: [2021-05-31 Mon 16:07]

Might as well just point at scr-beeb/beebniccc

*** DONE =run= HTTP method doesn't autoboot
    CLOSED: [2021-06-06 Sun 22:25]

Could be the autoboot mechanism? Not sure what the actual bug here is.

*** TODO =SDLBeebWindow::SetBeeb= should call completion funs

See =BeebThread::ThreadReplaceBeeb=

** changelog notes

#+BEGIN_SRC markdown
  - the `call` HTTP method has been removed
  - the `run` HTTP method no longer supports C64-style .PRG files
#+END_SRC
