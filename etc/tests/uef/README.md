Some random compressed UEFs.

To add more:

1. copy compressed UEF(s) to this folder
2. run `make_uncompressed_uefs.py` to create the uncompressed files
3. add file(s) to the `TestLoadPossiblyGzippedFile` test in `b2_test.cpp`
